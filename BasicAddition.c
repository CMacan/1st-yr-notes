#include <stdio.h>

int main()
{
int num1,num2, sum=0;

printf("Enter a number:");
scanf("%d", &num1);

printf("Enter 2nd number:");
scanf("%d", &num2);

sum=num1+num2;

printf("The sum of both numbers is %d", sum);

return 0;
}
